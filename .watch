{
  "build": [{
    "patterns": ["src/**/*.js"],
    "command": "npm run build:js -- --only",
    "appendFiles": true
  }, {
    "patterns": ["src/**/*.js"],
    "command": "npm run build:flow"
  }],
  "test": [{
    "patterns": ["src/**/*.js"],
    "command": "npm run lint -s"
  }]
}
